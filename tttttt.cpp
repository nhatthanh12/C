#include<stdio.h>
int main () {
	printf("Nhap so hang và cot cua hcn: ");
	int n,m;
	scanf("%d%d", &n, &m);
	for (int i=0; i<n; i++) {
		printf("%d", i);
		for (int j=0; j<m; j++) {
			if(j<m-i){
				printf("%d", i+j);
				}
				else{
				}
			
	
