#include<stdio.h>
#include<math.h>
int main() {
	int m, n;
	scanf("%d", &m, &n);
	for (int i = 1; i < m; i++) {
	
	scanf("%d", &n); }
	int can = sqrt(n);
	if (can * can == n) {
		printf ("Yes\n");
	 }else  printf ("No");
	 

return 0;
}
	
